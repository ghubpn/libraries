// ARCHIVE

/*
	Graph* g = new Graph(4);
	g->addEdge(1,3);
	g->addEdge(1,3);
	//g->addBiEdge(0,2);
	//g->addBiEdge(0,2);
	//g->addBiEdge(3,2);
	g->print();
	//cout << "check: " << g->checkEdge(0,2) << endl;
	*/
	
	